spring:
  application:
    name: cloud-gateway
  cloud:
    config:
      uri: ${CONFIG.SERVER.HOSTNAME:http://localhost:9000}
      profile: cer
      enabled: true

spring:
  application:
    name: product-service
  cloud:
    config:
      uri: ${CONFIG.SERVER.HOSTNAME:http://localhost:9000}
      profile: cer
      enabled: true